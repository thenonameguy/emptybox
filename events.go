package main
/*
import (
	"github.com/BurntSushi/xgbutil"
	"github.com/BurntSushi/xgbutil/keybind"
	"github.com/BurntSushi/xgbutil/xevent"
	"os/exec"
)
/*
func handleKeyPress(X *xgbutil.XUtil, ev xevent.KeyPressEvent) {
	mods := ev.State
	key_code := ev.Detail

	switch {
	case keybind.KeyMatch(X, "F12", mods, key_code):
		drawMenu(X, 0, 0, 12.0)
		cmd := exec.Command("xterm")
		err := cmd.Start()
		checkError(err)
	case keybind.KeyMatch(X, "F11", mods, key_code):
		cmd := exec.Command("gmrun")
		err := cmd.Start()
		checkError(err)
	default:
		break
	}
}*/
